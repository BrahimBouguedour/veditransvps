{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python3.9 -m ensurepip && python3.9 -m pip install --upgrade pip && cd backend && python3.9 -m pip install wheel setuptools && python3.9 -m pip install -r requirements.txt"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "cd backend && python3.9 -m uvicorn main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/",
    "healthcheckTimeout": 100
  }
} 