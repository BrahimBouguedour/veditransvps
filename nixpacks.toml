[phases.setup]
nixPkgs = ['python39', 'gcc', 'ffmpeg', 'git']
aptPkgs = ['python3-dev', 'build-essential']

[phases.install]
cmds = [
    'python3.9 -m pip install --upgrade pip',
    'cd backend',
    'python3.9 -m pip install wheel setuptools',
    'python3.9 -m pip install -r requirements.txt'
]

[start]
cmd = 'cd backend && python3.9 -m uvicorn main:app --host 0.0.0.0 --port $PORT'

[variables]
PYTHONUNBUFFERED = "1"
PYTHONPATH = "/app/backend" 